This folder contains automated/manual test artifacts produced during the SQE run on 2026-05-21.

Contents:
- `e2e-summary.txt` — human-readable summary of the manual E2E run (registration, login, submit complaint, admin actions).
- `localStorage_snapshot.json` — snapshot of `students_data`, `admins_data`, `complaints_data`, and `current_user` taken after the run (not always present if page access prevented).

Notes:
- Screenshots were captured during the interactive session and are available in the chat transcript. If you want them persisted in the repo, tell me and I will add them under this folder.
